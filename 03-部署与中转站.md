---
title: "03-部署与中转站"
created: 2026-08-23
updated: 2026-08-23
tags: [huanxin-ai, deploy, relay, ecs]
---

# 03 · 部署与中转站（2026-08 实战记录）

## 拓扑
```
用户浏览器
   │  (Cloudflare Tunnel 临时 URL，每次 cloudflared 重启变)
   ▼
huanxin-relay  (New API 中转站, :3000, 容器)
   │  OpenAI 兼容 /v1
   ▼
huanxin-ai   (huanxin-ai:local, :8000, 容器)
   │  容器内网 http://huanxin-relay:3000/v1
   ▼
智谱 glm-4-flash  (实际 LLM)
```

## 关键事实（ECS 实测）
- **huanxin-ai 镜像真实 env 名**：`OPENAI_BASE_URL` / `OPENAI_API_KEY` / `OPENAI_MODEL` / `HUANXIN_LLM_PROVIDER`
  —— **不是** 设计文档写的 `HUANXIN_RELAY_*`（文档与镜像脱节，以镜像 inspect 为准）
- 默认 `HUANXIN_LLM_PROVIDER=mock` → 用 Agnes 网关 `https://apihub.agnes-ai.com/v1`，模型 `agnes-2.0-flash`
- 数据卷 `huanxin-data` → `/app/data`（持久化，勿删）

## 中转站（New API）配置要点
- 渠道按 **「优先级 Priority」数字越小越优先**；同优先级按 **权重 Weight** 比例
- 中转站本质是"按模型名 → 找支持该模型的渠道"，请求 `glm-4-flash` 只命中智谱渠道
- **智谱渠道填法**：渠道类型选 `OpenAI`（勿选"智谱"老类型）；API 地址 `https://open.bigmodel.cn/api/paas/v4/`（**末尾斜杠必带**）；Key 去 bigmodel.cn 申请（GLM-4-Flash 实名后免费）
- **不要点顶部全局「获取模型」按钮**——会轰炸所有空壳渠道集体报错；要单渠道保存后再点该条的测试/获取

## 重启 core 接中转站（定型命令）
```bash
docker stop huanxin-ai && docker rm huanxin-ai
docker run -d --name huanxin-ai --restart unless-stopped \
  -p 8000:8000 -v huanxin-data:/app/data \
  -e HUANXIN_LLM_PROVIDER=openai \
  -e OPENAI_BASE_URL=http://huanxin-relay:3000/v1 \
  -e OPENAI_API_KEY=<中转站令牌 sk-wlJ2...> \
  -e OPENAI_MODEL=glm-4-flash \
  huanxin-ai:local
```

## Cloudflare Tunnel（临时暴露）
```bash
# 脱离 Workbench 会话保活（关键三件套）
setsid nohup cloudflared tunnel --url http://127.0.0.1:3000 \
  > /tmp/cf.log 2>&1 < /dev/null &
disown
grep -oE "https://[a-z0-9-]+\.trycloudflare\.com" /tmp/cf.log | head -1
```
- ⚠️ Quick Tunnel URL **每次重启随机**，进程死即 1033 错误
- 长期方案：Cloudflare **命名 Tunnel** + `cloudflared service install`（免费，URL 固定）

## 网络限制
- 用户本机（联通 27.147.x）↔ ECS 公网整体不可达（安全组已开）；靠 Tunnel 绕
- ECS 出境被挡：OpenAI / Gemini / models.dev 直连超时 → 海外渠道不可用，只用国内（智谱/DeepSeek/阿里百炼）
