---
title: "04-会话记录-2026-08"
created: 2026-08-23
tags: [emperor-core, session-log, deploy]
---

# 04 · 会话记录（2026-08 云部署轮）

> 本会话从"拍板架构 6 项"转向**实际云部署 + 中转站 + 量化 + 积分设计**。

## 时间线
- **08-21~22**：在沙箱写"中转站 + emperor-core + 量化模型接线 + 积分计费中间件 + 部署配置"，推 GitHub `kiwoen/emperor-core`（ECS 无 Docker，代码跑在用户云服务器）
- **08-23 17:20 起**：用户明确四项决策 ↓
  1. 先弄线上云服务器（实际部署）
  2. 本地部署模型降配（保留高质量但量化：GPTQ/AWQ 4-bit，13B→8GB）
  3. **中转站语义重定义**（核心）：平台统一支付 / 积分倍率 / 全模型接入 / 使用中学习 / 边界=推理层不实时借用外部
  4. 计费：免费额度 + 增值
- **08-23 18:30**：诊断 ECS SSH timeout 根因 = `/root` 权限 0550 导致 OpenSSH 拒登；用阿里云控制台 Workbench 网页版绕过
- **08-23 晚**：ECS 实际部署 emperor-core + emperor-relay；本机访问不通 → Cloudflare Tunnel 绕；配智谱 glm-4-flash

## 团队分工（software-emperor-*）
- PM（许清楚）：量化 VLM 调研 → `docs/quantized-vlm-deploy-2026-08-23.md`
  - 结论：**Qwen2.5-VL-7B-AWQ**（质量优先）或 **Qwen2.5-VL-3B-AWQ**（显存优先）替代 LongCat-Video
- 架构师（高见远）：积分中转站设计 → `docs/emperor-relay-billing-design-2026-08-23.md` + 2 张 mermaid
  - 4 语义：平台兜底账单 / 积分倍率 / 全模型接入 / 学习中转蒸馏中枢（旁路异步，不在推理路径）
  - 待拍板 7 项（存储/免费额度/倍率归属/学习开关/结晶频率/规格/用户ID）

## 工程现实
- Connector 现状：EdgeOne Pages（仅静态）、CloudBase（有容器但 New API=Go+SQLite 形态待确认）、GitHub（仅托管）
- **真实可行路径**：沙箱写代码+单测+推 GitHub → 用户 ECS `git pull` + 跑 `deploy/ecs/setup-ecs.sh`

## 待办（闭环）
- [x] 隧道复活（benefits-assumed-easy-fellow.trycloudflare.com）
- [x] 中转站容器存活确认
- [ ] 用户拿智谱 Key → 配智谱渠道 → 测通
- [ ] 重启 emperor-core 接中转站（glm-4-flash）
- [ ] 命名 Tunnel 固定 URL（长期）
- [ ] T06-T09 积分/学习代码落地（仍仅文档）
