# Security Policy

## 支持版本

以下版本目前接收安全更新：

| 版本 | 支持状态 |
|------|----------|
| 2.x   | 活跃支持 ✅ |
| 1.x   | 已停止 ⛔ |

## 报告安全漏洞

如果你发现了安全漏洞，**请不要在公开 Issue 中报告**。请按以下流程处理：

### 报告渠道

- 请向项目维护者发送私密邮件（详见仓库维护者联系方式）
- 或使用 GitHub 的 [Security Advisories](https://github.com/<org>/<repo>/security/advisories/new) 功能提交私密报告

### 报告要求

为使安全团队快速理解并修复问题，请尽量包含：

1. **漏洞描述**：清晰描述漏洞类型（如 RCE、权限提升、注入、信息泄露等）
2. **影响范围**：受影响的模块/组件及版本号
3. **复现步骤**：最小化的复现用例或 POC
4. **潜在影响**：攻击者利用该漏洞可造成的最严重后果
5. **修复建议**（可选）：如果你有修复方案的思路

### 处理流程

1. **确认收到**：48 小时内确认收到报告
2. **评估**：安全团队评估严重程度和影响范围
3. **修复**：开发补丁并进行内部测试
4. **披露**：修复发布后，根据严重程度决定是否发布安全公告（CVE）

### 响应时间

| 严重程度 | 目标响应时间 | 目标修复时间 |
|----------|------------|------------|
| Critical | 24 小时 | 72 小时 |
| High | 48 小时 | 7 天 |
| Medium | 1 周 | 30 天 |
| Low | 2 周 | 下个发布周期 |

## 安全最佳实践

### 对使用者

1. **及时升级**：始终使用最新稳定版，及时应用安全补丁。
2. **API 密钥管理**：不要将 API 密钥（OpenAI / Anthropic / 其他 LLM 服务）硬编码到代码中，使用环境变量或 `.env` 文件。
3. **网络隔离**：Dashboard 端口默认绑定 `127.0.0.1`，请勿在生产环境暴露到公网。如需外部访问，务必配置 HTTPS 和认证反向代理。
4. **依赖审查**：定期使用 `pip-audit` 或 `safety` 检查依赖项中的已知漏洞。

```bash
pip install pip-audit
pip-audit
```

### 对贡献者

1. **输入校验**：所有外部输入（用户提交、API 响应、文件读取）必须进行校验和清洗。
2. **凭据安全**：严禁在代码、注释、日志或测试文件中硬编码凭据。
3. **依赖引入**：新增依赖前评估其安全状况（维护活跃度、已知漏洞、许可证兼容性）。
4. **代码审查**：涉及安全敏感路径（认证、授权、加密、反序列化、命令执行）的 PR 必须由至少一位 Maintainer 进行安全审查。
5. **禁止 Eval**：禁止使用 `eval()` 或 `exec()` 处理用户可控数据（项目中的 `math` 能力使用 AST 安全求值，请保持该模式）。

### 安全敏感模块

以下模块涉及较高安全风险，修改时请格外谨慎：

- `jarvis/emperor.py` — 任务编排与执行入口
- `jarvis/plugins/` — 动态插件加载机制
- `jarvis/court/` — 大臣进化与权限模型
- `jarvis/mcp/` — MCP 协议与跨进程通信
- 任何涉及文件系统读写或外部命令执行的模块

## 致谢

我们感谢以负责任方式报告安全问题的研究人员和社区成员。经确认的安全漏洞报告者（如同意）将在安全公告中署名致谢。
