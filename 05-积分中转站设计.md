---
title: "05-积分中转站设计"
created: 2026-08-23
tags: [emperor-core, billing, relay, design]
---

# 05 · 积分中转站设计（架构师产出，2026-08-23）

> 完整设计见 `docs/emperor-relay-billing-design-2026-08-23.md`（主设计 470 行 + class-diagram.mermaid + sequence-diagram.mermaid）

## 四条核心语义
1. **平台统一兜底账单**：复用 New API 作"账单执行面"，持平台级单一令牌（`EMPEROR_RELAY_KEY`），用户不可见/无需各自 key
2. **积分倍率**：自建积分层（`BillingMiddleware` + `UserCreditAccount` + `ModelRateTable`），按 `token × 倍率` 扣用户积分，与 New API 渠道配额解耦
3. **全模型接入 + 用户自由选择**：`ModelCatalog` 从中转站 `/v1/models` 拉 + 合并倍率 → 前端下拉；`route_explicit` 用户显式优先于 RoleRouter/ConfidenceGate
4. **使用中学习蒸馏中枢**：`LearningCollector` 旁路异步采集真实调用 → `DistillationStore`（仅真实写）；`DistillationCrystallizer` 离线平台学习层（candidate→verify→审批闸），**不在推理响应路径**

## 硬边界
- 推理服务层响应落地时**只走自身 + 中转站推理模型**，绝不实时借用外部替代自身
- `LearningCollector` 仅 write-side（采集中转日志，不回灌推理）

## 计费公式
```
调用积分 = ceil( (tok_in/1k * rate_in) + (tok_out/1k * rate_out) )
余额不足 → 结构化拒绝（不 5xx）
```

## 存储
- 默认 **SQLite**（sqlalchemy 已存在），预留 Redis 接口

## 云部署
- `deploy/emperor-core/docker-compose.yml` 多服务：new-api(3000) + emperor-core(8000) + 可选 ollama(11434) + 数据卷
- EdgeOne Pages / CloudBase 仅托管前端静态，不替代有状态后端
- emperor-core 零新硬依赖

## 任务分解（T06-T09 承接 T01-T05）
- T06 积分中间件（独立基础）
- T07 模型目录 + 动态路由（依赖 T06）
- T09 云部署（依赖 T06/T07/T08）
- T01→T08 学习中转采集（依赖 executor 稳定）
- 新增 13 文件 + 改 6 文件

## 待用户拍板 7 项
1. 积分存储 SQLite / Redis（建议 SQLite）
2. 免费额度（建议 1000）
3. 倍率谁定（代码 JSON / 运营后台）
4. 学习中转实时/异步 + 合规开关（`EMPEROR_LEARNING_OPT_IN?`）
5. 蒸馏结晶频率 + 审批闸
6. 云服务器最低规格 + ollama 是否必选
7. user_id 来源（前端 / API token / 匿名）+ 是否联动 token_guard
