---
title: "02-代码地图"
created: 2026-08-23
tags: [huanxin-ai, code-map]
---

# 02 · 代码地图（关键文件速查）

> 仓库根约 4400 文件，以下为**真正需要理解**的入口，其余为依赖/测试/产物。

## 根目录关键文件
| 文件 | 作用 |
|---|---|
| `main.py` | 旧 uvicorn 裸跑入口（已被 docker-compose 的 `huanxin.cli` 取代） |
| `huanxin/cli.py` | **生产入口** `python -m huanxin.cli serve` |
| `docker-compose.yml` | 多服务编排（core 8000 + 可选 ollama 11434） |
| `Dockerfile` | 容器构建（基于 Python，装 huanxin_core） |
| `deploy.sh` / `setup.sh` / `setup.bat` | 部署脚本 |
| `pyproject.toml` / `requirements*.txt` | 依赖 |
| `render.yaml` | Render.com 部署配置 |
| `.env.example` | **配置模板**（真实 `.env` 已被排除，防密钥泄露） |

## `huanxin/` 核心子包
```
huanxin/
├── cli.py              # 命令行入口
├── core.py          # 皇帝核心协调器
├── core/               # 推理核心 + llm 抽象
│   └── llm/            # config / manager / core（OpenAI 兼容）
├── capabilities/       # 能力注册与调度
├── governance_agent.py # 治理智能体
├── court/              # 审计法庭（kg / orchestration / facade）
├── audit.py            # 审计落库
├── approval.py         # 审批闸门
├── evolution/          # 自进化算法
├── eval/ / eval_bench/# 评估基准
├── hierarchical_memory.py
├── llm_judge.py        # LLM 自评
├── hallucination_*.py  # 幻觉检测/护栏
└── api/                # FastAPI 路由
```

## 设计产物（docs/，非主代码）
- `docs/quantized-vlm-deploy-2026-08-23.md` —— 量化 VLM 部署调研（Qwen2.5-VL）
- `docs/huanxin-relay-billing-design-2026-08-23.md` —— 积分中转站设计（见 [[05-积分中转站设计]]）
- `docs/DEPLOY_STUDENT_ALIYUN.md` / `DEPLOY_CLOUD_VM.md` —— 部署指南
- `deploy/ecs/setup-ecs.sh` —— 阿里云 ECS 一键部署脚本

## 测试
`tests/` 下约 100+ 单测，命名 `test_*.py`，覆盖审批/审计/能力/CLI/API 等。
